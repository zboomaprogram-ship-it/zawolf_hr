# Request Visibility

The request-visibility feature provides bounded, role-scoped, legacy-aware
read models for request history, approval queues, and confirmed deductions.
It must not perform mutations or expose provider diagnostics to users.
