# Configurable requests

This migrated slice implements HR-defined request types, employee eligibility,
one-to-four-step approval routes, and approver notifications. It is delivered
under the reviewed umbrella specification `010-meeting-custom-requests`.

Definitions are snapshotted into each request; the operations runtime is the
authoritative writer and decision processor.
