# Attendance check-in feature lifecycle

The canonical specification, reviewed plan, task list, contract, research, and
validation steps for this migrated feature live in
[`../002-attendance-reliability/`](../002-attendance-reliability/).

This bridge intentionally satisfies the repository convention of one
`specs/<feature>/` entry for the `lib/features/attendance_checkin/` slice without
duplicating the source-of-truth documentation.
