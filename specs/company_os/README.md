# company_os

Specification source of truth: [`specs/005-internal-company-os/`](../005-internal-company-os/).

This pointer directory exists so the architecture guard
(`test/architecture_guard_test.dart`, "every migrated feature has a
specification directory") can bind `lib/features/company_os` to its reviewed
specification without moving the numbered spec directory.
